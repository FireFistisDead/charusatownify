<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title><%= type === 'lost' ? 'Lost Item' : 'Found Item' %> - Charusat Ownify</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('partials/header', { user: (user || null) }) %>

  <div class="container">
    <div class="card">
      <h4><%= item.title %></h4>
      <p><strong>Category:</strong> <%= item.category %></p>
      <p><strong>Description:</strong> <%= item.description %></p>
      <p><strong>Location:</strong> <%= item.location %></p>
      <p><strong>Date:</strong> <%= type === 'lost' ? (item.dateLost ? new Date(item.dateLost).toLocaleDateString() : '') : (item.dateFound ? new Date(item.dateFound).toLocaleDateString() : '') %></p>
      <a class="btn secondary" href="/">Back</a>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
